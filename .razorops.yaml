global:
  runner:
    os_image: ubuntu

tasks: 
  unit-tests:
    runner: SravaniBikkina/staticwebsite
tasks:
   build-static-website:
    steps:
      - checkout
      - build/website:
         htmlfile: index.html
         
      - junit: test-reports/*.xml
      - html:
          dir: htmlcov
